这个integration 层主要是zoom 客户端的事，我们这里POC 可以不管，先用最简单的方式模拟它。
这个模块完全独立实现，所有代码放在一个文件夹下


数据存储模式使用 Mongodb

说白了就是增删改查一个巨大的上下文文档数据。以最简单的方式暴露这个功能即可，每次提交时就是包含具体用户名，和新增的上下文数据。

另外在这个层额外实现这个主题的创建，以及主题内人员的创建。也是简单结构化数据。

对各个数据实体的增删改查以 MCP tool的形式暴露和组织，同时也支持 API 暴露。